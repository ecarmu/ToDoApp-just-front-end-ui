import React from "react";
import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        marginTop: 'auto',
        flexDirection: 'row',
    },
    title_container: {
        flexDirection: 'row',
        flex: 1
    },
})